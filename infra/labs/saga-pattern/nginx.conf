upstream saga_lab {
  server app:3000;
}

server {
  listen 80;

  location / {
    proxy_pass http://saga_lab;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Request-Id $http_x_request_id;
  }
}
